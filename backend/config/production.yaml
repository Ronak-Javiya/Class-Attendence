# ============================================
# Production Environment Configuration
# ============================================
# Committed to repo - no secrets here
# Secrets live in secrets/production.yaml

server:
  env: production
  # Port can be overridden via PORT env var if needed
  port: ${PORT:-5000}

aiService:
  # AI service URL should be set via environment variable in production
  url: ${AI_SERVICE_URL}
