# ============================================
# Production Secrets - EXAMPLE FILE
# ============================================
# Copy to production.yaml and fill in real values
# This example file IS committed to repo
# The actual secrets file is NOT committed
# Can use environment variables for secrets in production

jwt:
  access:
    secret: ${JWT_ACCESS_SECRET}
    expiry: 15m
  refresh:
    secret: ${JWT_REFRESH_SECRET}
    expiry: 7d
